import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sportconnect.app',
  appName: 'Sport Connect',
  webDir: 'dist/public'
};

export default config;
